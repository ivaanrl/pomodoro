import {
  startTimer,
  pauseTimer,
  resetTimer,
  increaseBreakTimer,
  increaseWorkTimer,
  decreaseBreakTimer,
  decreaseWorkTimer,
  hardResetTimer
} from './timer';

const allActions = {
  startTimer,
  pauseTimer,
  resetTimer,
  increaseBreakTimer,
  increaseWorkTimer,
  decreaseBreakTimer,
  decreaseWorkTimer,
  hardResetTimer
};

export default allActions;
